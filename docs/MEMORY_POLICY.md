# 记忆读取策略

新对话不得依赖模型自行决定是否读取记忆。创建会话时由应用强制执行：

1. 全局安全约束
2. 项目最高优先级规则
3. 仓库和模块规则
4. 当前任务规则
5. 相关历史证据

每次加载都记录 memory_load_event，包含记忆 ID、版本、优先级、适用范围、是否冲突和注入结果。加载失败必须显式显示，不得静默继续。

普通聊天中的猜测不能自动升级为项目规则。规则必须由用户确认，且有版本、更新时间、适用版本和失效条件。冲突规则必须展示并要求选择或标记待验证。
